// Вьшодит таблицу умножения на 7 
#include <stdio.h> 
#include <conio.h> 
void mainO 
{ 
int m; // число, для которого надо вьгоести 
// таблицу умножения (множимое) 
nt п; // множитель 
int р; // произведение 
m = 7; 
printf("ХпТаблица умножения на %i\n", m); 
for (n = 1; n<=9; n++) 
{ 
p = Ш * n; 
printf("%ix%i=%i\n", m, n, p); 
} 
printf("\пДля завершения нажмите <Enter>"); 
getch(); 
}
