// Таблица функции 
#inclucle <stdio.h> 
#include <conio.h> 
tdefine LB -2.0 // нижняя граница диапазона изменения 
// аргумента 
#define НВ 2.0 // верхняя граница диапазона изменения 
// аргумента 
tdefine DX 0.5 // приращение аргумента 
void main О 
{ 
float х,у; // аргумент и значение функции 
int п; // кол-во точек 
int i; // счетчик циклов 
п = (НВ - LB)/DX +1; 
X = LB; 
printf (" \n"); 
printf(" X I y\n"); 
printf (" \n") ; 
for (i = 1; i<=n; i++) 
{ 
у = -2.4*x*x+5*x-3; 
printf("%6.2f j %6.2f\n" ,K ,y); 
X += DX; 
} 
printf (" \n") ; 
printf("\пДля завершения нажмите <Enter>"); 
getch(); 
} 
